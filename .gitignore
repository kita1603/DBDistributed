build/
build_vs/
data/
*.exe
*.obj
*.o
.vs/

# Visual Studio's own CMake integration config - IDE- and machine-
# specific (compiler paths, debug args), analogous to config/routing.conf
CMakeSettings.json
launch.vs.json

# VS Code debug config - also has a machine-specific absolute gdb path
.vscode/

# Local scratch state from manual raftnode testing (raft log, kv data)
node1/
node2/
node3/
single_test/
txn_test/
local_test/

# Personal notes, not part of the project source
DBDistributed.docx

# Claude Code session tooling data
.claude/

# Local shard routing config - IPs/ports/shard layout are environment-
# specific, meant to be hand-edited per deployment rather than tracked
config/routing.conf
